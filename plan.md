- [] in time-table menu, here we can dynamically added the classes and section, teachers, subjects, duration of classes, lunch break time, class start time and end time, no. of classes, and how many day in a week a class is running. 

- [] make sure same teacher can't teach two subjects at same time. 

- [] there should be two buttons that is generate time-table (download time-table. generate time-table will download the time-table as pdf.) and another is view time-table.